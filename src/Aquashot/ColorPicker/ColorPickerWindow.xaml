<Window x:Class="Aquashot.ColorPicker.ColorPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="#01000000"
        ShowInTaskbar="False" Topmost="True" ResizeMode="NoResize" Cursor="Cross"
        MouseMove="OnMouseMove" MouseLeftButtonUp="OnMouseUp" KeyDown="OnKeyDown">
    <Canvas x:Name="Overlay">
        <Image x:Name="FrozenImage" Stretch="Fill"/>
        <Border x:Name="Readout" Visibility="Collapsed" CornerRadius="8" Padding="8"
                Background="{StaticResource B.Surface}" BorderBrush="{StaticResource B.Border}" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect BlurRadius="14" ShadowDepth="2" Opacity="0.5" Color="#000000"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal">
                <Border x:Name="Swatch" Width="26" Height="26" CornerRadius="5"
                        BorderBrush="{StaticResource B.Border}" BorderThickness="1"/>
                <TextBlock x:Name="HexText" Text="#000000" Margin="9,0,2,0" VerticalAlignment="Center"
                           FontFamily="Consolas" FontSize="14" Foreground="{StaticResource B.Text}"/>
            </StackPanel>
        </Border>
    </Canvas>
</Window>
